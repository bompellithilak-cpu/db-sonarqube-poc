[pytest]
# testpaths keeps `pytest` fast and unambiguous in CI.
testpaths = tests
# Add src/ to the import path so tests import dq_framework without an install step.
pythonpath = src
addopts = -q --strict-markers
